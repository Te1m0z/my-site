import { Toaster } from 'react-hot-toast'
import type { ReactElement } from 'react'

const AppToaster = (): ReactElement => {
  return (
    <Toaster
      toastOptions={{}}
    />
  )
}

export {
  AppToaster,
}
